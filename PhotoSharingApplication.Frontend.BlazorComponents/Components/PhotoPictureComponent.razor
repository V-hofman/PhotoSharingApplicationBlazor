<MudCardMedia Class="rounded-lg ma-4" Style=" max-block-size:50%; min-height:100; min-width:100; border:solid 1px; margin-left:10%; margin-right:10%; " Image="@(Photo!.PhotoFile is null ? "" : $"data:{Photo.ImageMimeType};base64,{Convert.ToBase64String(Photo.PhotoFile)}")" />	

@code {
    [Parameter]
    public Photo? Photo { get; set; }
}